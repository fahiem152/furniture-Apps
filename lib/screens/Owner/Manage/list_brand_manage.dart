import 'package:flutter/material.dart';

class ListBrandManage extends StatelessWidget {
  const ListBrandManage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Center(child: Text('ListBrandManage')),
      ),
    );
  }
}
