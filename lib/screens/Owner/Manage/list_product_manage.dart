import 'package:flutter/material.dart';

class ListProductManage extends StatelessWidget {
  const ListProductManage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Container(
        child: Text('ListProductManage'),
      ),
    );
  }
}
