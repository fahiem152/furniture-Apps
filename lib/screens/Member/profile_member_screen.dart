import 'package:flutter/material.dart';

class ProfileMemberScreen extends StatelessWidget {
  const ProfileMemberScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
        child: Center(
      child: Text('ProfileMemberScreen'),
    ));
  }
}
